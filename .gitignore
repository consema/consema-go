# consema-go: minimal .gitignore. The Go toolchain builds into its own cache
# and produces no in-tree artifacts, so nothing needs ignoring here.

# IDE / OS artifacts
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
